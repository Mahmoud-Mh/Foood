# ===========================================
# Recipe App Backend Environment Variables
# ===========================================

# Application Configuration
NODE_ENV=development
PORT=3001

# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=recipe_user
DATABASE_PASSWORD=your_strong_password_here
DATABASE_NAME=recipe_app

# JWT Configuration
# Generate strong secrets using: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_key_here_make_it_very_long_and_secure
JWT_EXPIRATION=1d
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key_here_also_make_it_very_long_and_secure
JWT_REFRESH_EXPIRATION=7d

# Rate Limiting Configuration
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# Frontend URL Configuration
FRONTEND_URL=http://localhost:3000

# Email Configuration (Optional - uses Ethereal test accounts in development)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM_ADDRESS=noreply@recipehub.com
EMAIL_FROM_NAME=Recipe Hub

# Production Configuration (only needed for production)
PRODUCTION_DOMAIN=https://your-production-domain.com
CORS_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com

# Optional: Logging Configuration
LOG_LEVEL=info
LOG_FILE_ENABLED=false
LOG_FILE_PATH=./logs/app.log

# Optional: Database Logging
DATABASE_LOGGING=false

# ===========================================
# Development Notes:
# ===========================================
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env files to version control
# 3. Generate strong JWT secrets using secure methods
# 4. Use environment-specific values for production
# 5. Ensure database credentials match your local setup
# ===========================================